/** Matching stub for AsyncStorage — see react-native.js. */
module.exports = {
  default: {
    getItem: async () => null,
    setItem: async () => {},
    removeItem: async () => {},
    multiRemove: async () => {},
  },
};

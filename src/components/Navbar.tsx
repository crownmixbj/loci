/**
 * Public name for the persistent header. The implementation stays in
 * `ui/app-nav-bar.tsx` — this is the entry point layouts import, so the two
 * header pieces (`Navbar`, `LiveTicker`) sit side by side in `components/`.
 */
export { AppNavBar as Navbar } from '@/components/ui/app-nav-bar';
